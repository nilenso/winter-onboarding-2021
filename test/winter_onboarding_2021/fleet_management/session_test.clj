(ns winter-onboarding-2021.fleet-management.session-test
  (:require [clojure.test :refer [deftest is testing]]))

(deftest read-session
  (testing "Should return the user id using a session id"
    (is true)))
